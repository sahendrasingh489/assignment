// Arrow function to calculate the square of a number
const square = (number) => number * number;

// Example: calculating the square of 5
const result = square(5);
console.log(`The square of 5 is: ${result}`);
